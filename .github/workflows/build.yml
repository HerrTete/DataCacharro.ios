name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build
        run: |
          xcodebuild -project SavedMessages.xcodeproj \
            -scheme SavedMessages \
            -destination "generic/platform=iOS Simulator" \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Run UI Tests
        run: |
          xcodebuild -project SavedMessages.xcodeproj \
            -scheme SavedMessages \
            -destination "platform=iOS Simulator,name=iPhone 17 Pro" \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            -skip-testing:SavedMessagesUITests/ShareExtensionUITests \
            -skip-testing:SavedMessagesUITests/SafariShareUITests \
            test
